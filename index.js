function arrangeCircles(sheetWidth, sheetHeight, circleDiameter) {
    let positions = [];
    let row = 0;
    let col = 0;
    let totalCircles = 0;
    
    while (row * circleDiameter < sheetHeight) {

